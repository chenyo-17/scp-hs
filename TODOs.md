-[] remove `TfAssignNull` if possible

-[] restructure `TfAssign` as an assocation list

-[] specify the key of `TfAssignItem` is always `TfVar`

-[] add log levels

-[] pretty printer as sympy

-[] reduce the router function to a single symbolic prefix